# Typemap for Minijinja XS module  
MODULE = Minijinja    PACKAGE = Minijinja  

IV      T_IV
  
struct mj_env*  T_IV


INPUT
size_t
        $var = (size_t)SvUV($arg);

OUTPUT
T_IV
        $var = (SV*)$arg;

T_PV  
        sv_2mortal(newSVpv($var, strlen($var)));

void    T_VOID

